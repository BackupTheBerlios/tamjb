# $Id$
# A replacement makefile for mp3sharp that strong-names the assembly

#
# Build and sign all third-party dll's that aren't supplied with
# keys. 
#
all:
	make --directory=dojo
	make --directory=mp3sharp
	make --directory=esd-sharp
